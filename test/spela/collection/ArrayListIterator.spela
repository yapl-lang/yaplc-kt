package spela.collection

import spela.collection.ArrayList

class ArrayListIterator(val list: ArrayList, val range) {
    fun hasNext -> Boolean = range->hasNext()
    fun next = list[range->next()]
}
