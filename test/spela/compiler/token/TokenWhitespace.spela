package spela.compiler.token

import spela.compiler.token.Token

class TokenWhitespace(val value: String) : Token() {
	val isWhitespace = true

	fun toString = "Whitespace(\"" ~ this->value ~ "\")"
}
