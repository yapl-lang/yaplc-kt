package yapl.compiler.token

import yapl.compiler.token.Token

class TokenLineComment(val value: String) : Token() {
	val isWhitespace = true

	fun toString = "LineComment(\"" ~ this->value ~ "\")"
}
